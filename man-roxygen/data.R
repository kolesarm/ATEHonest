#' @param alpha Level of confidence interval, \code{1-alpha}.
#' @param beta The quantile \code{beta} of excecss length for
#'     determining performance of one-sided CIs.
#' @param y outcome vector with length \code{n}
#' @param d Vector of treatment indicators with length \code{n}
#' @param sigma2 vector of variance estimates with length \code{n}
#' @param C Lipschitz smoothness constant
